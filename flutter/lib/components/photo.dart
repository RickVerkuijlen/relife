import 'package:flutter/material.dart';
class Photo{
  String image;
  String title;
  String description;
  String tags;
  String location;

  Photo(
      this.image, this.title, this.description, this.tags, this.location
      );
}